name: wizard-mongos-router-dn

networks:
  master-netwrk:
    external: true

services:
  wizard-mongos-router-dn:
    container_name: ${APP_NAME}_mongos-router-dn
    build:
      context: .
      dockerfile: Dockerfile
    restart: always
    volumes:
      - ./keyfile:/etc
      - wizard-mongos-router-dn-da:/data/db
      - wizard-mongos-router-dn-cf:/data/configdb
      - ./init/cluster.js:/init/cluster.js:ro
    env_file: ../../.env
    ports:
      - ${MONGOS_ROUTER_DN_PORT}:${MONGOS_ROUTER_DN_PORT_OF}
    networks:
      - master-netwrk

volumes:
  wizard-mongos-router-dn-da:
  wizard-mongos-router-dn-cf:
