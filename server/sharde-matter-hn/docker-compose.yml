name: wizard-sharde-matter-hn

networks:
  master-netwrk:
    external: true

services:
  wizard-sharde-matter-hn:
    container_name: ${APP_NAME}_sharde-matter-hn
    build:
      context: .
      dockerfile: Dockerfile
    restart: always
    volumes:
      - ./keyfile:/etc
      - wizard-sharde-matter-hn-da:/data/db
      - wizard-sharde-matter-hn-cf:/data/configdb
    env_file: ../../.env
    ports:
      - ${SHARDE_MATTER_HN_PORT}:${SHARDE_MATTER_HN_PORT_OF}
    networks:
      - master-netwrk

volumes:
  wizard-sharde-matter-hn-da:
  wizard-sharde-matter-hn-cf:
