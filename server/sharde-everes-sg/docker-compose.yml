name: sharde-everes-sg

networks:
  master-netwrk:
    external: true

services:
  sharde-everes-sg:
    container_name: ${APP_NAME}_sharde-everes-sg
    build:
      context: .
      dockerfile: Dockerfile
    volumes:
      - ./keyfile:/etc
      - sharde-everes-sg-da:/data/db
      - sharde-everes-sg-cf:/data/configdb
    env_file: ../../.env
    ports:
      - ${SHARDE_EVERES_SG_PORT}:${SHARDE_EVERES_SG_PORT_OF}
    networks:
      - master-netwrk
    command: |
      bash -c "
        set -e

        echo 'WAITING FOR KEYFILE...'
        while [ ! -f /etc/mongo-keyfile ]; do sleep 2; done
        echo 'KEYFILE CREATED'

        mkdir -p /data/db

        echo 'STARTING UP SHARDE-EVERES-SG IN PROGRESS WITH NO AUTH'
        mongod --replSet everesRS --bind_ip_all --port ${SHARDE_EVERES_SG_PORT_OF} --dbpath /data/db --fork --logpath /tmp/mongod.log --shardsvr
        echo 'THE SHARDE-EVERES-SG HAVE FINISHED BOOTING UP WITH NO AUTH'

        echo 'WAITING FOR STARTING UP SHARDE-EVERES-HN IN PROGRESS WITH AUTH'
        until mongosh --quiet --host ${SHARDE_EVERES_HN_ADRS}:${SHARDE_EVERES_HN_PORT_OF} -u ${MONGO_ADMINIST_SHARDE_EVERES} -p ${MONGO_PASSWORD_SHARDE_EVERES} --authenticationDatabase admin --eval 'db.adminCommand({ ping: 1 })' > /dev/null 2>&1; do sleep 2; done
        echo 'THE SHARDE-EVERES-HN HAVE FINISHED BOOTING UP WITH AUTH'

        echo 'WAITING FOR STARTING UP SHARDE-EVERES-SG IN PROGRESS WITH AUTH'
        mongod --shutdown --dbpath /data/db
        while pgrep mongod >/dev/null; do sleep 2; done
        mongod --replSet everesRS --bind_ip_all --port ${SHARDE_EVERES_SG_PORT_OF} --dbpath /data/db --auth --keyFile /etc/mongo-keyfile --setParameter enableLocalhostAuthBypass=0 --fork --logpath /tmp/mongod.log --shardsvr
        until mongosh --quiet --host ${SHARDE_EVERES_SG_ADRS}:${SHARDE_EVERES_SG_PORT_OF} --eval 'db.adminCommand({ ping: 1 })' > /dev/null 2>&1; do sleep 2; done
        echo 'THE SHARDE-EVERES-SG HAVE FINISHED BOOTING UP WITH AUTH'

        tail -f /tmp/mongod.log
      "

volumes:
  sharde-everes-sg-da:
  sharde-everes-sg-cf:
