name: wizard-mongos-router-hn

networks:
  master-netwrk:
    external: true

services:
  wizard-mongos-router-hn:
    container_name: ${APP_NAME}_mongos-router-hn
    build:
      context: .
      dockerfile: Dockerfile
    restart: always
    volumes:
      - ./keyfile:/etc
      - wizard-mongos-router-hn-da:/data/db
      - wizard-mongos-router-hn-cf:/data/configdb
    env_file: ../../.env
    ports:
      - ${MONGOS_ROUTER_HN_PORT}:${MONGOS_ROUTER_HN_PORT_OF}
    networks:
      - master-netwrk

volumes:
  wizard-mongos-router-hn-da:
  wizard-mongos-router-hn-cf:
