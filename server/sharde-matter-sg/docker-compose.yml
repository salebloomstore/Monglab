name: sharde-matter-sg

networks:
  master-netwrk:
    external: true

services:
  sharde-matter-sg:
    container_name: ${APP_NAME}_sharde-matter-sg
    build:
      context: .
      dockerfile: Dockerfile
    restart: always
    volumes:
      - ./keyfile:/etc
      - sharde-matter-sg-da:/data/db
      - sharde-matter-sg-cf:/data/configdb
    env_file: ../../.env
    ports:
      - ${SHARDE_MATTER_SG_PORT}:${SHARDE_MATTER_SG_PORT_OF}
    networks:
      - master-netwrk

volumes:
  sharde-matter-sg-da:
  sharde-matter-sg-cf:
