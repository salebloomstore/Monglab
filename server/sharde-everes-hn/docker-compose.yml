name: wizard-sharde-everes-hn

networks:
  master-netwrk:
    external: true

services:
  wizard-sharde-everes-hn:
    container_name: ${APP_NAME}_sharde-everes-hn
    build:
      context: .
      dockerfile: Dockerfile
    restart: always
    volumes:
      - ./keyfile:/etc
      - wizard-sharde-everes-hn-da:/data/db
      - wizard-sharde-everes-hn-cf:/data/configdb
    env_file: ../../.env
    ports:
      - ${SHARDE_EVERES_HN_PORT}:${SHARDE_EVERES_HN_PORT_OF}
    networks:
      - master-netwrk

volumes:
  wizard-sharde-everes-hn-da:
  wizard-sharde-everes-hn-cf:
