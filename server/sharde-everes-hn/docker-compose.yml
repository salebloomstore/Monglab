name: sharde-everes-hn

networks:
  master-netwrk:
    external: true

services:
  sharde-everes-hn:
    container_name: ${APP_NAME}_sharde-everes-hn
    build:
      context: .
      dockerfile: Dockerfile
    restart: always
    volumes:
      - ./keyfile:/etc
      - sharde-everes-hn-da:/data/db
      - sharde-everes-hn-cf:/data/configdb
    env_file: ../../.env
    ports:
      - ${SHARDE_EVERES_HN_PORT}:${SHARDE_EVERES_HN_PORT_OF}
    networks:
      - master-netwrk

volumes:
  sharde-everes-hn-da:
  sharde-everes-hn-cf:
